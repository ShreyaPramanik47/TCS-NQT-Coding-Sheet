// Check if the number is abundant number or not
#include <bits/stdc++.h>
using namespace std;
bool abundant(int n){
    for(int i=1; i<=sqrt(n); i++){
        if(n%i==0)
    }
}
int main(){
    int n;
    cin>>n;
    bool ans= abundant(n);
    return 0;
}